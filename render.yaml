services:
  - type: web
    name: timetable-backend
    runtime: node
    rootDir: DataBase
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MONGO_URI
        sync: false
      - key: PORT
        value: 5000
